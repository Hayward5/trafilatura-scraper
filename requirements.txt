# Runtime dependencies for Trafilatura web scraping and text extraction
# Install with: pip install -r requirements.txt
# Note: Browser binaries still need to be installed separately with:
#       python -m playwright install chromium

certifi
charset_normalizer>=3.4.0
courlan>=1.3.2
htmldate>=1.9.2
justext>=3.0.1
lxml==4.9.2; platform_system == 'Darwin' and python_version <= '3.8'
lxml>=5.3.0; platform_system != 'Darwin' or python_version > '3.8'
urllib3>=1.26,<3
playwright>=1.40.0,<2
